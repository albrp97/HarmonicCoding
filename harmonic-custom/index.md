# Harmonic Custom Layer

- `AGENTS.md` — local overrides for the shared Harmonic Coding rules
- `config.yml` — repo-level workflow toggles
- `skills/` — reusable skill entrypoints that load the workflow prompts and guides
